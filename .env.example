# ---- Hostnames (Traefik) ----
PROJECT_SLUG=myapp

APP_HOST=${PROJECT_SLUG}.test
DB_UI_HOST=${PROJECT_SLUG}-db.test
VITE_HOST=${PROJECT_SLUG}-vite.test

# ---- Apache document root ----
# Laravel: /var/www/html/public
Vanilla/WP: /var/www/html
# APACHE_DOCUMENT_ROOT=/var/www/html/public

# ---- Optional .htaccess support ----
# Default is vhost-driven config (recommended):
ALLOW_OVERRIDE=None
# If you need .htaccess for a specific project:
# ALLOW_OVERRIDE=All

# ---- Database (per-project) ----
DB_HOST=db
DB_PORT=3306
DB_DATABASE=app
DB_USERNAME=app
DB_PASSWORD=app
DB_ROOT_PASSWORD=root

# ---- Frontend / Vite (optional) ----
ENABLE_FRONTEND=false
VITE_PORT=5173
